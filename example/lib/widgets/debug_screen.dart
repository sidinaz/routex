import 'package:flutter/material.dart';
import 'package:routex/routex.dart';

class DebugScreen extends StatelessWidget {
  final RoutingContext _context;

  DebugScreen(this._context);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
